import "./lazyLoadingLoader.css";

const LazyLoadingLoader = () => {
  return (
    <div className="loader-overlay">
      <p className="loading-text">Loading...</p>
    </div>
  );
};

export default LazyLoadingLoader;